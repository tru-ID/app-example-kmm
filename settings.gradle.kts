pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "PhoneCheckExample"
include(":PhoneCheckAndroidApp")
include(":shared")